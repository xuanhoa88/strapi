{
  "prefix": "/excels",
  "routes": [
    {
      "method": "POST",
      "path": "/create-by-template",
      "handler": "excel.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/download",
      "handler": "excel.download",
      "config": {
        "policies": []
      }
    }
  ]
}
